# Archived taxonomy tools

This folder keeps the previous taxonomy-based similarity system and related admin audits.

The active site now uses manually managed similar movies, so these files are intentionally not loaded by any page:

- `horror-taxonomy.js`
- `taxonomy-admin.js`

Keep them here as historical reference while the manual system remains the production path.
